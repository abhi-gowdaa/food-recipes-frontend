import React from 'react'
import { Box } from '@mui/material';

const Visualize = () => {
  return (
    <Box
    className="data-card"
      component="section" 
      sx={{
        p: 2,
        m: "auto",
        mt: "20px",
        overflow:"hidden",
        mb:"10px",
        maxWidth: "60rem",
        height: "540px",
        borderRadius: "25px",
       
      }}
    > 
         </Box>
  )
}

export default Visualize